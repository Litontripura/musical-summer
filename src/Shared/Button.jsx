

const Button = ({children}) => {
    return (
        <div className="cursor-pointer py-3 rounded-lg text-center font-bold bg-gradient-to-r from-primary to-blue-500 ...">{children}</div>
    );
};

export default Button;